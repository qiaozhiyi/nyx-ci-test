[package]
name = "nyx-client"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Nyx pure-Rust egui operator desktop client (no Node/JS) — same REST API as the CLI"

[dependencies]
eframe = "0.29"
ureq = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
anyhow = { workspace = true }

[[bin]]
name = "nyx-client"
path = "src/main.rs"
