# MOVED (WP-C crate split, 2026-08-08): this template now lives at
#   crates/implant-core/config.toml
# The implant's build-time config baking moved with `bake_config` into
# nyx-implant-core's build.rs, which reads CARGO_MANIFEST_DIR/config.toml.
# Editing THIS file has no effect on builds. Override explicitly with:
#   NYX_CONFIG=/abs/path/config.toml NYX_SERVER_PUB=<64hex> cargo +nightly build --release
