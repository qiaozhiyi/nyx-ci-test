[package]
name = "nyx-pe"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Nyx PE export-directory resolver — resolve Win32/NT API by name from a DLL image (PIC-implant foundation)"

[dependencies]

[lib]
name = "nyx_pe"
path = "src/lib.rs"
