{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the Nyx operator UI main window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
